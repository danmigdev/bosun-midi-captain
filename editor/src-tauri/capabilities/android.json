{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "android",
  "description": "Capability set required on Android. Grants the serialplugin permission for USB OTG serial communication with the MIDI Captain pedal.",
  "platforms": ["android"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "serialplugin:default"
  ]
}
